{
  "name": "verifiedtokyo-sync",
  "version": "1.0.0",
  "description": "Automated product sync from t-secondhands.jp to Verified Tokyo",
  "main": "scrape.js",
  "scripts": {
    "scrape": "node scrape.js",
    "upload": "node upload.js",
    "sync": "npm run scrape && npm run upload"
  },
  "dependencies": {
    "ftp": "^0.3.10",
    "dotenv": "^16.0.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
